---
title: Justin Leveck
pageable: true
per_page: 10
---
.row
  .twelve.columns
    - page_articles.each_with_index do |article, i|
      .panel.article
        %p
          %h3.post-title
            = link_to article.title, article
          %p.meta
            %span= article.date.strftime('%b %d, %Y')
          %article
            = article.body
          -#= link_to "Read More", article, class: 'button'

.row
  .six.columns
    - if prev_page
      .panel
        %p
          = link_to 'Previous page', prev_page

  .six.columns
    - if paginate
      - if next_page
        .panel
          %p= link_to 'Next page', next_page